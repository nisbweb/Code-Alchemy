-- Shift Register (4-bit)
-- Create a 4-bit shift register in VHDL. 
-- The shift register should take a serial input (SI), shift the bits either left or right based on a control signal (Shift_Right), and output the current 4-bit value (Q). 
-- It should shift on each rising clock edge.

-- Inputs:
-- SI: 1-bit serial input.
-- Shift_Right: 1-bit control signal (1 for right shift, 0 for left shift).
-- Clk: 1-bit clock input.

-- Output:
-- Q: 4-bit shift register output.

-- WRITE YOUR CODE HERE